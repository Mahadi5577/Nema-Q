# NEMA-R control on disease (H5 cross-dataset).
# Shares the disease tuned nemaq recipe (selected on validation).
inherit: ../base.yaml
data: { name: disease, split: ratio }
model: { name: nemaq, quantum_mode: frozen_random, gate_bias_init: -2.0 }
train: { lr: 0.05, aux_weight: 0.3 }
